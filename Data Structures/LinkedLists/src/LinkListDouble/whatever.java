package LinkListDouble;

public class whatever {
/*
	problem 2 in the exam:
	int temp = s.top();
	s.pop();
	while (!s.isEmpty()){
	if(s.top()<= temp)
	t.push(s.pop());
	else
	s.pop();
	
	}
	while(!t.isEmpty())
	s.push(t.pop())
	s.push(temp)
}

prblem3
public static  question 3:
